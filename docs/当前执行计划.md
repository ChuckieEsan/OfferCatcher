# OfferCatcher 当前执行计划

> 更新时间：2026-05-02
> 当前 Sprint：Sprint 1 — 记忆基础（Memory Phase 2：Agent 检索 + Hook 驱动）

---

## 背景

当前记忆系统的问题与目标：

| 现状 | 问题 | 目标 |
|------|------|------|
| MemoryRetrievalService 做 embedding → Qdrant → 拼格式的 RAG 管道 | 检索质量依赖向量相似度，无法理解用户意图 | MemoryRetrievalAgent：独立 Agent 分析意图 → 多角度检索 → 精炼输出 |
| ChatAgentService 手动调用 buildMemoryContext() + postProcess() | 记忆逻辑嵌在主流程中，不可复用 | Hook 驱动：MemoryInjectionHook + MemoryPostProcessingHook 解耦 |
| 无降级机制 | 缓存 miss 时上下文为空 | 降级到 importance top-K 查询（<10ms） |
| MemoryAgentService 无游标管理 | 每次处理所有消息，可能重复提取 | 增加 message_cursor 追踪 |

> **注意**：Memory Phase 1（AutoContextMemory）当前跳过。原因是 ChatAgentService 每次请求新建 ReActAgent，单次请求内的消息量有限（maxIters=10），token 不会爆炸。且 AutoContextMemory 扩展包需要额外验证可用性。如有长对话 token 问题再引入。

---

## 文件清单

### 新增文件

| # | 文件 | 说明 |
|---|------|------|
| 1 | `src/main/java/com/zju/offercatcher/application/agent/MemoryRetrievalAgent.java` | 记忆检索 Agent |
| 2 | `src/main/java/com/zju/offercatcher/infrastructure/memory/MemoryInjectionHook.java` | PreCall 记忆注入 Hook |
| 3 | `src/main/java/com/zju/offercatcher/infrastructure/memory/MemoryPostProcessingHook.java` | PostCall 记忆后处理 Hook |
| 4 | `src/test/java/com/zju/offercatcher/application/agent/MemoryRetrievalAgentTest.java` | 检索 Agent 测试 |
| 5 | `src/test/java/com/zju/offercatcher/infrastructure/memory/MemoryInjectionHookTest.java` | 注入 Hook 测试 |
| 6 | `src/test/java/com/zju/offercatcher/infrastructure/memory/MemoryPostProcessingHookTest.java` | 后处理 Hook 测试 |

### 修改文件

| # | 文件 | 改动说明 |
|---|------|---------|
| 7 | `application/agent/ChatAgentService.java` | 注册 Hook，移除手动 buildMemoryContext/postProcess |
| 8 | `application/service/MemoryRetrievalService.java` | 职责收缩为工具提供者 |
| 9 | `application/agent/MemoryAgentService.java` | 增加游标管理 |
| 10 | `domain/memory/repositories/SessionSummaryRepository.java` | 确认 findTopKByImportance 可用 |
| 11 | `infrastructure/config/MemoryProperties.java` | MemoryRetrievalService 改用配置类 |

---

## Step 1：MemoryRetrievalAgent 实现

### 1.1 核心逻辑

```
retrieveAsync(userId, conversationId, query, recentMessages)
  ↓
创建 ReActAgent（小模型, maxIters=5）
  ├── sysPrompt: "你是记忆检索专家..."
  ├── tools: search_session_history, load_memory_reference
  └── taskPrompt: 当前 query + 最近对话 + 检索策略建议
  ↓
Agent 自主执行：
  ├── 分析用户意图
  ├── search_session_history("RAG召回阈值设置")
  ├── search_session_history("向量检索调优")  // 多角度
  ├── load_memory_reference("preferences")    // 按需
  └── 整合精炼输出（≤500 tokens）
  ↓
写入 Redis: oc:memory:context:{userId}:{conversationId}, TTL=3600s
```

### 1.2 代码实现

```java
package com.zju.offercatcher.application.agent;

import com.zju.offercatcher.domain.chat.entities.Message;
import com.zju.offercatcher.infrastructure.common.CacheKeys;
import com.zju.offercatcher.infrastructure.config.LLMProperties;
import com.zju.offercatcher.infrastructure.tools.MemoryTools;
import com.zju.offercatcher.infrastructure.tools.UserToolContext;
import io.agentscope.core.ReActAgent;
import io.agentscope.core.message.Msg;
import io.agentscope.core.message.MsgRole;
import io.agentscope.core.model.GenerateOptions;
import io.agentscope.core.model.OpenAIChatModel;
import io.agentscope.core.tool.ToolExecutionContext;
import io.agentscope.core.tool.Toolkit;
import io.agentscope.core.tool.ToolkitConfig;
import org.slf4j.Logger;
import org.slf4j.LoggerFactory;
import org.springframework.data.redis.core.RedisTemplate;
import org.springframework.stereotype.Service;

import java.time.Duration;
import java.util.List;
import java.util.concurrent.CompletableFuture;

@Service
public class MemoryRetrievalAgent {

    private static final Logger log = LoggerFactory.getLogger(MemoryRetrievalAgent.class);
    private static final int MAX_ITERS = 5;
    private static final int CONTEXT_TTL_SECONDS = 3600;
    private static final int MAX_OUTPUT_TOKENS = 500;

    private final MemoryTools memoryTools;
    private final RedisTemplate<String, String> redisTemplate;
    private final OpenAIChatModel retrievalModel;
    private final Toolkit cachedToolkit;

    public MemoryRetrievalAgent(MemoryTools memoryTools,
                                RedisTemplate<String, String> redisTemplate,
                                LLMProperties llmProperties) {
        this.memoryTools = memoryTools;
        this.redisTemplate = redisTemplate;

        LLMProperties.DeepSeek cfg = llmProperties.getDeepseek();
        this.retrievalModel = OpenAIChatModel.builder()
            .apiKey(cfg.getApiKey())
            .modelName(cfg.getModel())
            .baseUrl(cfg.getBaseUrl())
            .stream(false)
            .build();

        this.cachedToolkit = new Toolkit(ToolkitConfig.defaultConfig());
        this.cachedToolkit.registerTool(memoryTools);
    }

    public void retrieveAsync(String userId, Long conversationId,
                               String query, List<Message> recentMessages) {
        CompletableFuture.runAsync(() -> {
            try {
                log.debug("MemoryRetrievalAgent started for conv {}", conversationId);
                ReActAgent agent = createRetrievalAgent(userId);
                String taskPrompt = buildTaskPrompt(query, recentMessages);
                Msg result = agent.call(List.of(
                    Msg.builder().role(MsgRole.USER).textContent(taskPrompt).build()
                )).block();

                String context = result.getTextContent();
                if (context != null && !context.isBlank()) {
                    String cacheKey = CacheKeys.memoryContext(userId, conversationId);
                    redisTemplate.opsForValue().set(
                        cacheKey, context, Duration.ofSeconds(CONTEXT_TTL_SECONDS));
                    log.info("Async retrieval cached: {} chars for conv {}",
                        context.length(), conversationId);
                }
            } catch (Exception e) {
                log.error("Async retrieval failed for conv {}: {}",
                    conversationId, e.getMessage(), e);
            }
        });
    }

    public String getCachedContext(String userId, Long conversationId) {
        return redisTemplate.opsForValue()
            .get(CacheKeys.memoryContext(userId, conversationId));
    }

    private ReActAgent createRetrievalAgent(String userId) {
        ToolExecutionContext toolContext = ToolExecutionContext.builder()
            .register("userContext", new UserToolContext(userId))
            .build();

        return ReActAgent.builder()
            .name("memory-retrieval")
            .sysPrompt("""
                你是记忆检索专家。你的任务：
                1. 分析用户问题，理解其核心意图
                2. 使用 search_session_history 从多个角度检索相关历史记忆
                3. 如果用户问题涉及偏好或行为规则，使用 load_memory_reference
                4. 整合搜索结果，输出一段精炼的上下文，供主 Agent 使用

                原则：
                - 从 2-3 个不同角度构造搜索查询
                - 只提取与当前问题真正相关的内容
                - 输出控制在 500 tokens 以内
                - 结果相关性低时换个角度重新搜索
                """)
            .model(retrievalModel)
            .toolkit(cachedToolkit)
            .toolExecutionContext(toolContext)
            .maxIters(MAX_ITERS)
            .generateOptions(GenerateOptions.builder()
                .temperature(0.1)
                .maxTokens(MAX_OUTPUT_TOKENS)
                .build())
            .build();
    }

    private String buildTaskPrompt(String query, List<Message> recentMessages) {
        StringBuilder sb = new StringBuilder();
        sb.append("## 任务\n检索与用户问题相关的历史记忆，整合为精炼的上下文。\n\n");
        sb.append("## 用户问题\n").append(query).append("\n\n");

        if (recentMessages != null && !recentMessages.isEmpty()) {
            sb.append("## 最近对话\n");
            for (int i = Math.max(0, recentMessages.size() - 6);
                 i < recentMessages.size(); i++) {
                Message m = recentMessages.get(i);
                String role = m.isUserMessage() ? "用户" : "AI";
                sb.append(role).append(": ")
                    .append(truncate(m.getContent(), 200)).append("\n\n");
            }
        }

        sb.append("## 检索策略\n");
        sb.append("1. 分析核心意图，构造多个不同角度的搜索查询\n");
        sb.append("2. 涉及\"怎么设置\"、\"推荐\"等，同时检查 preferences\n");
        sb.append("3. 结果不足时换个角度重新搜索\n");
        sb.append("4. 只提取真正相关的内容，输出 ≤ 500 tokens\n");
        return sb.toString();
    }

    private String truncate(String s, int maxLen) {
        return s != null && s.length() > maxLen ? s.substring(0, maxLen) + "..." : s;
    }
}
```

### 1.3 测试用例

**测试类**：`MemoryRetrievalAgentTest.java`

| 测试 | 场景 | 预期结果 |
|------|------|---------|
| `shouldCacheContextOnSuccessfulRetrieval` | Agent 成功检索并精炼 | Redis 中有对应 key 的缓存值，内容 ≤ 500 tokens |
| `shouldHandleEmptySearchResults` | Qdrant 无匹配结果 | Agent 返回空或"未找到相关记忆"，不抛异常 |
| `shouldNotBlockOnFailure` | LLM 调用超时/失败 | `retrieveAsync` 不抛异常，仅 log.error |
| `shouldGetCachedContext` | 已知 key 有缓存值 | `getCachedContext` 返回缓存内容 |
| `shouldReturnNullForMissingCache` | key 不存在 | `getCachedContext` 返回 null |
| `shouldUseMultipleSearchAngles` | 用户问"RAG阈值" | Agent 至少调用 2 次 search_session_history，查询词不同 |

---

## Step 2：MemoryInjectionHook 实现

### 2.1 核心逻辑

```
PreCallEvent 触发
  ↓
1. 读取 MEMORY.md（MemoryApplicationService.getMemoryContent）
  ↓
2. 读取 Short-term Memory（MemoryRetrievalAgent.getCachedContext）
  ├── 命中 → 注入 Agent 精炼的上下文
  └── miss → 降级：findTopKByImportance(userId, 5)，纯 SQL <10ms
  ↓
3. 合并为 memoryContext → 插入 inputMessages 最前面（SYSTEM 消息）
```

### 2.2 代码实现

```java
package com.zju.offercatcher.infrastructure.memory;

import com.zju.offercatcher.application.agent.MemoryRetrievalAgent;
import com.zju.offercatcher.application.service.MemoryApplicationService;
import com.zju.offercatcher.domain.memory.entities.SessionSummary;
import com.zju.offercatcher.domain.memory.repositories.SessionSummaryRepository;
import io.agentscope.core.hook.Hook;
import io.agentscope.core.hook.HookEvent;
import io.agentscope.core.hook.PreCallEvent;
import io.agentscope.core.message.Msg;
import io.agentscope.core.message.MsgRole;
import io.agentscope.core.message.TextBlock;
import reactor.core.publisher.Mono;

import java.util.ArrayList;
import java.util.List;

public class MemoryInjectionHook implements Hook {

    private final MemoryApplicationService memoryService;
    private final MemoryRetrievalAgent retrievalAgent;
    private final SessionSummaryRepository sessionSummaryRepository;
    private final String userId;
    private final Long conversationId;

    public MemoryInjectionHook(MemoryApplicationService memoryService,
                               MemoryRetrievalAgent retrievalAgent,
                               SessionSummaryRepository sessionSummaryRepository,
                               String userId, Long conversationId) {
        this.memoryService = memoryService;
        this.retrievalAgent = retrievalAgent;
        this.sessionSummaryRepository = sessionSummaryRepository;
        this.userId = userId;
        this.conversationId = conversationId;
    }

    @Override
    public int priority() {
        return 50; // 先于 ObservabilityHook (200)
    }

    @Override
    public <T extends HookEvent> Mono<T> onEvent(T event) {
        if (event instanceof PreCallEvent e) {
            String memoryContext = buildFullMemoryContext();
            if (memoryContext != null && !memoryContext.isBlank()) {
                List<Msg> messages = new ArrayList<>(e.getInputMessages());
                messages.add(0, Msg.builder()
                    .role(MsgRole.SYSTEM)
                    .content(List.of(TextBlock.builder().text(memoryContext).build()))
                    .build());
                e.setInputMessages(messages);
            }
            return Mono.just(event);
        }
        return Mono.just(event);
    }

    private String buildFullMemoryContext() {
        StringBuilder sb = new StringBuilder();

        // 1. MEMORY.md 概要（始终注入，~400 tokens）
        String memoryMd = memoryService.getMemoryContent(userId);
        if (memoryMd != null && !memoryMd.isBlank()) {
            sb.append(memoryMd);
        }

        // 2. Short-term Memory
        //    命中（~1ms）：Agent 精炼的上下文
        //    miss → 降级（<10ms）：重要性 top-K
        String shortTerm = retrievalAgent.getCachedContext(userId, conversationId);
        if (shortTerm == null || shortTerm.isBlank()) {
            shortTerm = buildDegradedContext();
        }
        if (shortTerm != null && !shortTerm.isBlank()) {
            if (!sb.isEmpty()) sb.append("\n\n---\n\n## 相关历史会话\n");
            sb.append(shortTerm);
        }

        return sb.toString();
    }

    private String buildDegradedContext() {
        List<SessionSummary> topK = sessionSummaryRepository
            .findTopKByImportance(userId, 5);
        if (topK.isEmpty()) return null;
        StringBuilder sb = new StringBuilder();
        for (SessionSummary s : topK) {
            sb.append("- ").append(s.getSummary()).append("\n");
        }
        return sb.toString();
    }
}
```

### 2.3 测试用例

| 测试 | 场景 | 预期结果 |
|------|------|---------|
| `shouldInjectMemoryContext` | Redis 缓存命中 | inputMessages 第 0 条为 SYSTEM 消息，含 MEMORY.md + 搜索上下文 |
| `shouldFallbackWhenCacheMiss` | Redis 缓存 null | inputMessages 含降级查询结果（importance top-5） |
| `shouldNotInjectWhenNoMemory` | MEMORY.md 不存在且缓存 miss 且降级为空 | inputMessages 不变 |
| `shouldNotModifyExistingMessages` | 已有 inputMessages | 原有消息保留在注入的 SYSTEM 消息之后 |
| `shouldRespectHookPriority` | 多个 Hook 注册 | MemoryInjectionHook(50) 先于其他 Hook 执行 |

---

## Step 3：MemoryPostProcessingHook 实现

### 3.1 核心逻辑

```
PostCallEvent 触发
  ↓
fire-and-forget (workerExecutor):
  1. MemoryRetrievalAgent.retrieveAsync(userId, conversationId, lastQuery, recentMessages)
  2. MemoryAgentService.extractMemories(userId, conversationId, messages)
```

### 3.2 代码实现

```java
package com.zju.offercatcher.infrastructure.memory;

import com.zju.offercatcher.application.agent.MemoryExtractionAgent;
import com.zju.offercatcher.application.agent.MemoryRetrievalAgent;
import com.zju.offercatcher.application.service.ChatApplicationService;
import com.zju.offercatcher.domain.chat.aggregates.Conversation;
import com.zju.offercatcher.domain.chat.entities.Message;
import io.agentscope.core.hook.Hook;
import io.agentscope.core.hook.HookEvent;
import io.agentscope.core.hook.PostCallEvent;
import reactor.core.publisher.Mono;

import java.util.List;
import java.util.concurrent.CompletableFuture;
import java.util.concurrent.Executor;

public class MemoryPostProcessingHook implements Hook {

    private final MemoryAgentService memoryAgent;
    private final MemoryRetrievalAgent retrievalAgent;
    private final ChatApplicationService chatService;
    private final Executor workerExecutor;
    private final String userId;
    private final Long conversationId;

    public MemoryPostProcessingHook(MemoryAgentService memoryAgent,
                                     MemoryRetrievalAgent retrievalAgent,
                                     ChatApplicationService chatService,
                                     Executor workerExecutor,
                                     String userId, Long conversationId) {
        this.memoryAgent = memoryAgent;
        this.retrievalAgent = retrievalAgent;
        this.chatService = chatService;
        this.workerExecutor = workerExecutor;
        this.userId = userId;
        this.conversationId = conversationId;
    }

    @Override
    public <T extends HookEvent> Mono<T> onEvent(T event) {
        if (event instanceof PostCallEvent e) {
            CompletableFuture.runAsync(() -> {
                // 1. 启动检索 Agent 预热 Short-term Memory
                String lastMsg = e.getFinalMessage().getTextContent();
                Conversation conv = chatService
                    .getConversation(userId, conversationId).orElse(null);
                List<Message> recentMessages = conv != null
                    ? conv.getMessages() : List.of();
                retrievalAgent.retrieveAsync(
                    userId, conversationId, lastMsg, recentMessages);

                // 2. 启动记忆提取 Agent 更新 Long-term Memory
                if (conv != null) {
                    memoryAgent.extractMemories(
                        userId, conversationId, conv.getMessages());
                }
            }, workerExecutor);
            return Mono.just(event);
        }
        return Mono.just(event);
    }
}
```

### 3.3 测试用例

| 测试 | 场景 | 预期结果 |
|------|------|---------|
| `shouldTriggerAsyncTasks` | PostCallEvent 触发 | retrieveAsync 和 extractMemories 被异步调用 |
| `shouldNotBlockEventChain` | PostCallEvent 处理 | onEvent 立即返回 Mono.just(event)，不阻塞 |
| `shouldHandleNullFinalMessage` | e.getFinalMessage() 为 null | 不抛 NPE |
| `shouldNotFailWhenConversationNotFound` | 对话不存在 | extractMemories 不调用，log 不抛异常 |

---

## Step 4：ChatAgentService 重构

### 4.1 改动点

**移除** `buildMemoryContext()` 方法——逻辑迁移到 MemoryInjectionHook。

**移除** `postProcess()` 中的记忆提取和检索触发——迁移到 MemoryPostProcessingHook。

**保留** `postProcess()` 中的消息保存和标题生成。

**新增** Hook 注册：

```java
// createReActAgent 中注册 Hook
private ReActAgent createReActAgent(String userId, Long conversationId) {
    // ... 现有逻辑不变 ...

    return ReActAgent.builder()
        .name("offer-catcher-chat")
        .sysPrompt(systemPrompt)
        .model(cachedModel)
        .toolkit(cachedToolkit)
        .toolExecutionContext(toolContext)
        .maxIters(10)
        .hooks(List.of(
            new MemoryInjectionHook(memoryService, retrievalAgent,
                sessionSummaryRepository, userId, conversationId),
            new MemoryPostProcessingHook(memoryAgent, retrievalAgent,
                chatService, workerExecutor, userId, conversationId),
            agentObservabilityHook
        ))
        .generateOptions(GenerateOptions.builder()
            .temperature(0.3).maxTokens(2048).build())
        .build();
}
```

**简化** `chatStream()` 方法——移除异步检索触发和 buildMemoryContext 调用，这些现在由 Hook 处理。

### 4.2 测试用例

| 测试 | 场景 | 预期结果 |
|------|------|---------|
| `shouldNotInjectMemoryIntoSystemPrompt` | createReActAgent 调用后 | systemPrompt 不再包含手动拼接的记忆内容 |
| `shouldStillSaveMessagesOnPostProcess` | 对话正常结束 | 消息和回复正确保存到 DB |
| `shouldStillGenerateTitleOnPostProcess` | 新对话达到 4 条消息 | 标题自动生成 |
| `shouldRegisterAllHooks` | 创建 ReActAgent | hooks 列表包含 MemoryInjectionHook、MemoryPostProcessingHook、ObservabilityHook |

---

## Step 5：MemoryRetrievalService 职责收缩

### 5.1 改动点

`MemoryRetrievalService` 当前包含检索决策逻辑（计算 embedding、Qdrant 搜索、合并上下文）。收缩为：

- **保留**：Redis 缓存管理（getCachedContext 现有逻辑）
- **保留**：检索锁（Redis SETNX）
- **保留**：底层 `searchByVector` 调用（作为 `MemoryTools.search_session_history` 的底层实现）
- **移除**：`triggerRetrieval()` —— 由 MemoryRetrievalAgent 替代
- **移除**：`buildMergedContext()` —— Agent 自行整合输出

注意：`MemoryTools.searchSessionHistory()` 当前直接调用 `sessionSummaryRepository.searchByVector()`，不经过 `MemoryRetrievalService`。所以 `MemoryRetrievalService` 的 `doRetrieval` 逻辑实际上与 Tool 无关——它只是被 ChatAgentService 的 `postProcess` 调用的独立检索路径。**可以标记 @Deprecated 后移除**。

### 5.2 测试用例

| 测试 | 场景 | 预期结果 |
|------|------|---------|
| `shouldStillManageCache` | getCachedContext 调用 | 正常返回 Redis 缓存值 |
| `shouldStillManageLock` | 检索锁获取释放 | 正常 SETNX + DEL |

---

## Step 6：MemoryAgentService 游标管理

### 6.1 改动点

当前 `extractMemories` 每次处理 conversation 的全部消息，不跟踪处理进度。增加：

```java
// 新增字段
private final MemoryCursorRepository cursorRepository;

public void extractMemories(String userId, Long conversationId,
                             List<Message> allMessages) {
    // 1. 读取上次游标
    Long lastCursor = cursorRepository.getCursor(userId, conversationId);

    // 2. 过滤游标后的新消息
    List<Message> newMessages = lastCursor == null
        ? allMessages
        : allMessages.stream()
            .filter(m -> m.getMessageId() > lastCursor)
            .toList();

    if (newMessages.isEmpty()) {
        log.debug("No new messages since cursor, skipping extraction");
        return;
    }

    // 3. 检查游标后是否有主 Agent 写入
    if (hasMemoryWriteMarker(newMessages)) {
        log.info("Memory update skipped: main agent already wrote");
        updateCursor(userId, conversationId, getLatestMessageId(newMessages));
        return;
    }

    // 4. 执行提取（现有逻辑，但只处理 newMessages）
    // ...

    // 5. 更新游标
    updateCursor(userId, conversationId, getLatestMessageId(allMessages));
}

private boolean hasMemoryWriteMarker(List<Message> messages) {
    return messages.stream()
        .filter(m -> m.getRole() == MessageRole.ASSISTANT)
        .anyMatch(m -> m.getContent() != null
            && m.getContent().contains("<memory_write>"));
}
```

游标存储在 Redis：`oc:memory:cursor:{userId}:{conversationId}`，值 = 最新处理的消息 ID。

### 6.2 测试用例

| 测试 | 场景 | 预期结果 |
|------|------|---------|
| `shouldOnlyProcessNewMessages` | 游标在消息 5，共 10 条消息 | 只处理消息 6-10 |
| `shouldSkipWhenNoNewMessages` | 游标在最新消息 | extractMemories 直接返回，log "skipping" |
| `shouldSkipWhenMemoryWriteMarkerExists` | 消息含 `<memory_write>` | 跳过后台提取，log "main agent already wrote" |
| `shouldUpdateCursorAfterExtraction` | 正常提取完成 | Redis 游标更新为最新消息 ID |
| `shouldProcessAllOnFirstRun` | 游标不存在（首次） | 处理所有消息 |

---

## Step 7：端到端集成测试

### 7.1 记忆注入测试

| 测试 | 场景 | 验证 |
|------|------|------|
| `shouldInjectMemoryInChatStream` | 用户发消息 → Agent 响应 → 再次发消息 | 第二次请求的 system prompt 包含上一轮的检索上下文 |
| `shouldFallbackWhenCacheMiss` | 首次对话无缓存 | system prompt 包含 importance top-5 摘要 |
| `shouldNotLoseMemoryWhenTopicSwitches` | 聊 RAG → 切换聊 Java | 降级兜底不会注入完全不相关的内容 |
| `shouldUpdateMemoryContextOnEachTurn` | 连续 3 轮对话 | 每轮结束触发异步检索，下一轮上下文更新 |

### 7.2 记忆提取测试

| 测试 | 场景 | 验证 |
|------|------|------|
| `shouldExtractPreferencesFromChat` | 用户说"每次回答都给出代码示例" | preferences.md 中新增 RAG 话题偏好 |
| `shouldNotExtractSimpleQA` | 用户问"Java volatile 是什么"，Agent 回答 | 不产生 session_summary（简单问答不满足 ≥3 轮条件） |
| `shouldWriteSummaryForDeepDiscussion` | 用户连续 3 轮追问 RAG 检索策略 | session_summaries 表新增一条记录 + Qdrant 向量 |
| `shouldNotDuplicateSummary` | 相似讨论重复出现 | 去重：不写入语义相似的摘要 |

### 7.3 回归测试

| 测试 | 验证 |
|------|------|
| 对话功能正常 | 用户消息 → Agent 响应，流式输出正常 |
| 工具调用正常 | search_questions, search_web, knowledge_graph 正常调用 |
| 消息保存正常 | 消息和回复正确写入 messages 表 |
| 标题生成正常 | 第 4 条消息后自动生成标题 |

---

## 验收标准

### Step 1 验收
- [ ] MemoryRetrievalAgent 创建成功，Agent 能调用 search_session_history
- [ ] 检索结果写入 Redis，缓存命中时 <1ms 读取
- [ ] 所有单元测试通过

### Step 2 验收
- [ ] PreCallEvent 触发后 inputMessages 第 0 条为记忆上下文
- [ ] 缓存 miss 时降级到 importance top-5
- [ ] 全部用户记忆为空时不注入空白消息

### Step 3 验收
- [ ] PostCallEvent 后异步触发检索 Agent 和记忆提取 Agent
- [ ] 异常不传播到事件链

### Step 4 验收
- [ ] ChatAgentService.chatStream() 功能正常，流式输出不受影响
- [ ] 消息保存和标题生成不受影响

### Step 5 验收
- [ ] MemoryRetrievalService 已标记 @Deprecated 的代码不再被调用
- [ ] 缓存管理功能保持正常

### Step 6 验收
- [ ] 重复对话不会重复提取记忆
- [ ] `<memory_write>` 标记正确触发互斥跳过

### Step 7 验收
- [ ] 三组端到端测试全部通过
- [ ] 回归测试无失败
